node_modules/
.chrome-profile/
screenshots/
playwright-report/
test-results/
*.png
*.jpg

# Secrets — never commit
.env

# Personal data — contains real name, email, phone; use profile.example.json as template
data/profile.json

# Runtime files generated by the automation — not for version control
data/automation.log
data/linkedin-applications.json
data/linkedin-retry-queue.json
data/nike-reviews.json

# Next.js build output (already in web/.gitignore, root-level safety net)
web/.next/
web/out/
